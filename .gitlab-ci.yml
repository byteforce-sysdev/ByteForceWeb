image: maven:3.6-ibmjava-8-alpine

variables:
   APP_VERSION: "1.0.0"

cache:
   paths:
      - node_modules/
      - vendor/

stages:
   - production

production:
  stage: production
  services:
    - docker:dind
  script:
    - echo "Uploading docker file build"
    - apk update && apk add --no-cache docker-cli
    - chmod +x ./docker_upload.sh
    - sh ./docker_upload.sh latest
  only:
    - tags
    - master
  tags:
    - gitlab-org-docker
